#!/usr/bin/env python3
#!/usr/bin/python

import apihelper
import math

phi = (1 + math.sqrt(5)) / 2
print("math = %s" % str(math))

apihelper.info(math)

print("math.pi = %s" % str(math.pi))
print("math.e = %s" % str(math.e))
print("phi = %s" % str(phi))
